# @agentnav/cli

Command line tools for AgentNav.

```bash
agentnav init
agentnav scan http://localhost:3000
agentnav score http://localhost:3000
agentnav build
```

AgentNav is the agent-readiness layer for React and Next.js.

See the main README: https://github.com/mido18/agentnav#readme
